#include <stdio.h>
#include <string.h>

int N, M, j;

int main() {
    scanf("%d %d", &N, &M);
    char InputStory[N], OutputStory[N];

    for (int i = 0; i < N; i++) {
        scanf("%[\n]s", &InputStory[i]);
    }

}
